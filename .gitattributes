# Gunakan LF untuk semua file teks secara default
* text=auto eol=lf

# Gunakan CRLF untuk file batch Windows
*.bat text eol=crlf

# File gambar ditandai sebagai biner agar tidak diubah oleh Git
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mp4 binary

# Jika TIDAK menggunakan Git LFS, tandai font sebagai biner biasa
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary

# Jika MENGGUNAKAN Git LFS, gunakan baris berikut dan pastikan Git LFS sudah diinisialisasi
# *.ttf filter=lfs diff=lfs merge=lfs -text
# *.otf filter=lfs diff=lfs merge=lfs -text
# *.woff filter=lfs diff=lfs merge=lfs -text
# *.woff2 filter=lfs diff=lfs merge=lfs -text

/.editorconfig  export-ignore
/.gitattributes export-ignore
/.gitignore     export-ignore
/README.md      export-ignore
